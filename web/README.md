# Dashboard

Next.js (static export) console for the Voice-Agent API: playground, voices, API keys, usage, docs, admin.

    npm install
    npm run dev      # http://localhost:3000, talks to the API on :8000 (see .env.development)
    npm run build    # writes ./out, which the FastAPI server serves at /
